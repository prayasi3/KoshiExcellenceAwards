# The deployment platform normally supplies PORT. Do not hard-code it.
PORT=5000
HOST=0.0.0.0
NODE_ENV=production

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=replace_with_a_secret

# Comma-separated list of public sites permitted to call this API.
FRONTEND_URL=https://koshiexcellenceaward.com
